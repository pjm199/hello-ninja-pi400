#pragma once

// Somma da 0 a 9 (esempio semplice)
int compute_sum_0_to_9();

// Somma intera da a a b (inclusi).
// Se a > b, scambia internamente.
int sum_range(int a, int b);

// Fattoriale di n (n >= 0).
// Per valori negativi, restituisce -1 come errore.
long long factorial(int n);
